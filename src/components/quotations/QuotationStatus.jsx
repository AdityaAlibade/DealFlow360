// TODO: Implement QuotationStatus badge/stepper component
import React from 'react';

const QuotationStatus = ({ status }) => {
  // TODO: Draft, Approval Pending, Approved, Sent, Accepted, Rejected
  return (
    <div>
      {/* TODO: Build quotation status UI */}
    </div>
  );
};

export default QuotationStatus;
