// TODO: Implement ProductForm component for creating/editing products
import React from 'react';

const ProductForm = ({ initialData, onSubmit }) => {
  // TODO: Handle form inputs: name, SKU, price, stock, category
  return (
    <div>
      {/* TODO: Build product form UI */}
    </div>
  );
};

export default ProductForm;
