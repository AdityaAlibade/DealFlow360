// TODO: Implement MetricCard for KPI display
import React from 'react';

const MetricCard = ({ title, value, change, trend, icon }) => {
  // TODO: Build metric display with trend indicators
  return (
    <div>
      {/* TODO: Build metric card UI */}
    </div>
  );
};

export default MetricCard;
