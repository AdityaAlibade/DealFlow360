// TODO: Implement CartItem row in quotation builder
import React from 'react';

const CartItem = ({ item, onUpdate, onRemove }) => {
  // TODO: Handle item discount, custom price, delete
  return (
    <div>
      {/* TODO: Build cart item row UI */}
    </div>
  );
};

export default CartItem;
